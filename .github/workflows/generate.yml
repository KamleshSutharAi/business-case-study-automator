name: Generate Business Documentary

on:
  workflow_dispatch:
    inputs:
      topic:
        description: 'Business Case Study Topic'
        required: true
        default: 'The Collapse of Toys R Us'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install Python Dependencies
        run: pip install google-genai requests edge-tts

      - name: Run Video Generator
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          VIDEO_TOPIC: ${{ github.event.inputs.topic }}
        run: python generate_video.py

      - name: Upload Finished Video as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: finished-documentary
          path: final_documentary.mp4
